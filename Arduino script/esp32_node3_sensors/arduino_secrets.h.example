#pragma once
// Template for Wi-Fi credentials.
// Copy this file to "arduino_secrets.h" (same folder) and fill in your network.
// arduino_secrets.h is gitignored, so your real credentials stay out of git.
#define SECRET_WIFI_SSID "your-wifi-name"
#define SECRET_WIFI_PASS "your-wifi-password"
